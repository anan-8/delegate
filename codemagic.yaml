workflows:
  ios-workflow:
    name: iOS Build Workflow
    environment:
      vars:
        BUNDLE_ID: \"com.anan8.delegate\"
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: main
          include: true
    scripts:
      - name: Clean and setup
        script: |
          flutter clean
          flutter pub get

      - name: Build without CocoaPods
        script: |
          rm -rf ios
          flutter build ios --release --no-codesign --no-pub --verbose

      - name: Check build results
        script: |
          if [ -d \"build/ios\" ]; then
            echo \"✅ Build successful! Files:\"
            find build/ios -name \"*.app\" -o -name \"*.xcarchive\" 2>/dev/null || echo \"No app files but directory exists\"
          else
            echo \"❌ Build failed - no build directory\"
            exit 1
          fi

    artifacts:
      - build/ios/
      - build/ios/**/*
